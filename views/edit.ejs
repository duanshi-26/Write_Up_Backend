<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Note</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-zinc-100 min-h-screen flex items-center justify-center">
    <div class="form bg-zinc-200 rounded-lg shadow-lg p-8 w-full max-w-md">
        <h2 class="text-xl font-bold text-zinc-800 mb-6 text-center">Edit Note</h2>
        <form action="/edit" method="POST">
            <label class="block text-zinc-700 mb-2" for="title">Title</label>
            <input type="text" id="title" placeholder="Previous Title" class="block w-full mb-4 px-4 py-2 border border-zinc-400 rounded focus:outline-none focus:ring-2 focus:ring-zinc-500 bg-zinc-50 text-zinc-800" value="<%= filename %>" name="previous" readonly>
            <label class="block text-zinc-700 mb-2" for="details">Details</label>
            <textarea name="new" id="details" placeholder="Edit your note..." class="block w-full mb-4 px-4 py-2 border border-zinc-400 rounded focus:outline-none focus:ring-2 focus:ring-zinc-500 bg-zinc-50 text-zinc-800"></textarea>
            <input type="submit" value="Update" class="bg-zinc-700 text-white px-4 py-2 rounded hover:bg-zinc-800 cursor-pointer w-full">
        </form>
        <a href="/" class="block mt-4 text-zinc-600 underline hover:text-zinc-900 text-sm text-center">Back to Home</a>
    </div>
</body>
</html>